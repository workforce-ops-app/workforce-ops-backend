# User Documentation — Backend API

**In short:** how to use the backend's API, for frontend developers and anyone integrating with it. End-user guides (for employees, managers, administrators, and owners) live in the frontend repository.

> Pending: written as endpoints are added. The generated reference is in [../api/](../api/README.md).

## Topics

| Topic | Status |
|---|---|
| Signing in and sessions | to do |
| CSRF tokens on state-changing requests | to do |
| Errors and status codes | to do |
| Pagination and filtering | to do |
| API versioning (`/api/v1`) | to do |
